# UCAT Reflex

UCAT Reflex is a static Progressive Web App for UCAT training across VR, DM, QR, and SJT.

## Local preview

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## GitHub Pages

This project is ready to deploy as a static site on GitHub Pages from the repository root.
